<%= simple_form_for [@episode, @character] do |f| %>
  <%= f.input :first_name %>
  <%= f.input :last_name, required: false %>
  <%= f.input :quote, required: false %>
  <%= f.submit "Add", class: 'btn btn-primary'%>
<% end %>
